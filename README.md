this project use deepface library to detect emotion. <br>
use pip install -r requirements.txt to download all the necessary libraries
<br> also you need to edit "haarcascade_frontalface_default.xml" file path
